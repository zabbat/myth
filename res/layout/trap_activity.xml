<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/activity_background_color"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".TrapActivity" >

    <LinearLayout
        android:id="@+id/trap_bait_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:clickable="true"
        android:layout_alignParentTop="true"
        android:background="#80808080"
         >
	
        <ImageView
            android:id="@+id/trap_bait_image"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/bait_icon_x" 
            android:layout_margin="5dp"
            android:layout_weight="1"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/activity_text_color" 
            android:text="Create Bait"
            android:layout_margin="5dp"
            android:layout_weight="1"
            />
        
    </LinearLayout>

    <fragment
        android:layout_below="@id/trap_bait_layout"
        android:layout_alignParentBottom="true"
        android:id="@+id/trap_list_frag"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        class="net.wandroid.mythology.trap.TrapListFragment" />

</RelativeLayout>